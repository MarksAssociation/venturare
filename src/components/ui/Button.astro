---
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'ghost' | 'text';
  size?: 'sm' | 'md' | 'lg';
  type?: 'button' | 'submit' | 'reset';
  class?: string;
  target?: '_blank' | '_self';
  rel?: string;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  type = 'button',
  class: className = '',
  target,
  rel,
} = Astro.props;

const baseClasses = 'inline-flex items-center justify-center gap-2 font-button rounded transition-all duration-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-venturare-green focus-visible:ring-offset-2 focus-visible:ring-offset-venturare-black disabled:opacity-50 disabled:cursor-not-allowed';

const variantClasses = {
  primary: 'bg-venturare-green text-venturare-black hover:bg-venturare-green/90 active:bg-venturare-green-dim',
  secondary: 'border border-venturare-border text-venturare-muted hover:border-venturare-green/50 hover:text-venturare-white active:border-venturare-green active:text-venturare-green',
  ghost: 'text-venturare-muted hover:text-venturare-green active:text-venturare-green-dim',
  text: 'text-venturare-muted hover:text-venturare-green gap-1.5',
};

const sizeClasses = {
  sm: 'px-4 py-1.5 text-button',
  md: 'px-5 py-2.5 text-button',
  lg: 'px-7 py-3 text-button',
};

const classes = `${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${className}`;
---

{href ? (
  <a href={href} class:list={[classes, variant === 'text' && 'group']} target={target} rel={rel}>
    <slot />
    {variant === 'text' && <span class="transition-transform duration-200 group-hover:translate-x-0.5">&rarr;</span>}
  </a>
) : (
  <button type={type} class={classes}>
    <slot />
  </button>
)}
