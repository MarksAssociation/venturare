---
interface Props {
  href: string;
  class?: string;
  target?: '_blank' | '_self';
  rel?: string;
}

const { href, class: className = '', target, rel } = Astro.props;
---

<a
  href={href}
  class:list={['text-muted hover:text-accent transition-colors duration-150 underline underline-offset-4 decoration-border/40 hover:decoration-accent/60', className]}
  target={target}
  rel={rel}
>
  <slot />
</a>
