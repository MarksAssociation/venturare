---
interface Props {
  text: string;
  class?: string;
}

const { text, class: className = '' } = Astro.props;
---

<span class:list={['text-caption text-venturare-muted/70', className]}>
  #{text}
</span>